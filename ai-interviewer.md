Here's the overall user flow first, then a mockup for each screen.Now the setup screen — the entry point where the user picks their interview config.Next, the interview screen — this is where the user spends most of their time, one question at a time with a visible progress indicator.Finally, the results screen — overall score up top, then per-question breakdown with feedback.That covers the three core screens and how they connect. A few UX notes worth carrying into the React build: the progress bar on the interview screen gives the candidate a sense of how much is left, which reduces anxiety; the topic/difficulty tag stays visible throughout so context isn't lost; and on the results screen, color-coding scores (green for strong, amber for weak) lets the user scan for weak spots at a glance without reading every line.

One implementation detail to flag for the frontend: after "Submit answer," there's a brief LLM wait — worth adding a simple loading state (e.g., disable the button, show "Scoring...") on that screen so it doesn't feel frozen.

Want me to also mock up a "scoring in progress" state for the interview screen, or move on to scaffolding the actual FastAPI/React code?